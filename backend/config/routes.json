{
    "routes": [
      {
        "method": "GET",
        "path": "/test/email",
        "handler": "test.sendTestEmail",
        "config": {
          "policies": []
        }
      }
    ]
  }